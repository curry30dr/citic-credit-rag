# 中信银行信用卡智能咨询助手 · 答辩讲稿（5分钟版）

---

各位老师好，我汇报的项目是**中信银行信用卡智能咨询助手**。

**【背景】**（约40秒）

信用卡客服有三个痛点：第一，业务规则多且数字敏感，取现、年费、还款这些规则客户问法千变万化；第二，资料分散在领用合约、价格表、章程里；第三，通用大模型不懂本行规则，直接问它会编数字。我们的方案是 RAG——不重新训练模型，把银行业务资料外挂给大模型，先检索到可信资料，再让大模型基于资料回答，做到每个数字都有据可查。

**【数据处理】**（约50秒）

原始资料约2.5万字，我们切成 **89块知识库**。切片策略是每块500字、相邻重叠80字，这个参数做了消融实验：固定overlap=80测了200-800七种chunk-size，30题Recall@4在500字最高88%；固定chunk=500测了0-160九种overlap，80字最优。按句子边界切，不把一句话切断。价格表里每个费用项独立成块，避免"取现手续费"和"违约金"混在一块导致召回歧义。这里踩过一个坑：价格表里有合并单元格，一开始用 python-docx 解析时，把"取现手续费"和它下面合并的多行内容当成了独立单元格重复提取，同一段文字重复进了4个块，把"取现手续费"这个关键词稀释了，导致问"取现手续费多少"召回出来的块里根本没有正确费率。后来改成按表格真实行列顺序交错提取段落和表格行，重复问题才解决。

**【技术架构】**（约1分30秒，重点）

整体是两路并行召回：一路向量召回，负责语义理解，比如"忘记还款"≈"逾期"；一路 BM25 关键词召回，负责精确匹配"年费""取现"这种专有名词。两路各取Top10。

两路分数量纲不同不能直接加，我们用 RRF 倒数排名融合，k=60，只看排名不看分数，不用调权重。融合后走 Rerank 精排，Cross-Encoder 逐对判断相关性，把最相关的顶到前面，取Top 5喂给 qwen-plus。prompt 里强制它只依据给定资料回答，temperature 设0.2降低发散。

技术栈做了双栈：本地用 bge-small-zh 加 bge-reranker-base，离线零成本；在线用阿里百炼的 text-embedding-v3 加 gte-rerank-v2，免维护。生成模型统一用 qwen-plus，保证对比公平。

**【实验效果】**（约50秒）

我们自己出了20道真实业务题，对比四种配置的 Recall@4：纯向量85%，纯BM25 90%，混合RRF 90%，**混合加Rerank 95%**。结论是 Rerank 是真正拉开差距的那一步。老师给的3道统一测试题也全部答对，关键数字与原始资料一致。

**【演示与总结】**（约50秒）

（切浏览器）这是网页界面，中信红风格，客户输入问题后系统检索资料回答，可展开看召回来源。

总结一下：这个项目交付了89块知识库、双栈RAG系统、消融实验和可对话网页。最大的收获是——RAG本质是工程问题，数据清洗和切块策略决定上限，模型只是最后那一步。

我的汇报到这里，谢谢老师，请提问。

